
  # Design prompt for HealHer (Community)

  This is a code bundle for Design prompt for HealHer (Community). The original project is available at https://www.figma.com/design/d7WHj6MI1CQR8sSiwVQYzW/Design-prompt-for-HealHer--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  